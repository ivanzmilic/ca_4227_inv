import numpy as np 
import matplotlib.pyplot as plt

# The function reads an RH model atmosphere and outputs a SNAPI model atmosphere
# RH has no magnetic fields so that will be zero 
# RH only has electrons, hydrogen populations but we need pressure. 

